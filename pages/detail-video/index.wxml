<!--pages/detail-video/index.wxml-->
<video class="video" 
        src="{{mvURL.url}}"
        loop
        autoplay>
</video>

<block wx:for="{{100}}" wx:key="*this">
  <view>List{{item}}</view>
</block>

